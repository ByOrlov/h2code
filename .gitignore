/h2code
/h2code-remote
*.dwarf
/node_modules
/lib
/bin
/.shard.lock
/.shards/

kimi-code/
*.swp
.rake_tasks~

tmp/
VOICE-INTEGRATION.md

*.deb

# miniaudio build artifacts (source files are tracked, objects are not)
vendor/miniaudio/*.o
vendor/miniaudio/*.a
vendor/miniaudio/*.obj
vendor/miniaudio/*.lib
vendor/miniaudio/.bridge_stamp

# scratch space for cloned reference repos (codex/opencode/grok-cli)
/temp/
# Vagrant machine-local state (contains private_key)
.vagrant/
*.dll
*.exe
*.exe.old
*.exe.prev
*.pdb
tmp_lmstudio_test.json

# self-hosted GitLab runner state (contains the registration token)
/ci/runner/data/
